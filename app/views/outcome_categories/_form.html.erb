<div class="control-group">
  <%= f.label :name, 'שם', class: "control-label"%>
  <div class="controls">
    <%= f.text_field :name, class: "input-block-level"%>
  </div>
</div>

<h2>הוצאות</h2>
<%= f.fields_for :params do |pa| %>
  <%= render partial: 'param_fields' , locals: {f: pa}%>
<% end %>
<%= link_to_add_fields "הוסף הוצאה", f, :params %>